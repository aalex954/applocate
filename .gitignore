# Core build artifacts
bin/
obj/
[Bb]uild/
[Dd]ebug/
[Rr]elease/
*x64/
*x86/
*.user
*.suo
*.userosscache
*.sln.docstates

# VS / VS Code
.vs/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Rider / JetBrains
.idea/
*.iml

# Dotnet intermediates
project.lock.json
project.fragment.lock.json
artifacts/
.nuget/packages/*
# (keep) allow lock file generation, do not ignore global packages.

# Logs / diagnostics
*.log
logs/
TestResult*/
*.VisualState.xml
*.coverage
*.coveragexml
coverage/

# Generated JSON or dumps
*.dump
*.diag
*.dmp

# Publish / distribution
publish/
*.publish.xml
*.pubxml
*.pubxml.user

# Cache / temp
*.tmp
*.temp
~$*
[Tt]emporary[Aa]sp[Nn]et[Ff]iles/

# OS / tooling noise
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*.swp

# Security / secrets (none expected; placeholder)
secrets.json
*.pfx
# Additional sensitive / key material
*.pem
*.key
*.cer
*.crt
*.der
*.snk
*.p12
*.pfx.json
*.keystore
*.jks
*.ovpn

# Environment & dotenv style files
.env
.env.*
*.env
*.env.local
*.dotenv

# Local override appsettings (keep base & environment defaults tracked)
appsettings.*.local.json
appsettings.Local.json
appsettings.Development.local.json

# Backup / patch artifacts
*.bak
*.orig
*.rej
*.patch

# Credential / secret folders (if ever created)
.secrets/
secrets/

# Encrypted vault / password store formats
*.kdbx

# SQLite / lite dbs (explicit opt‑out per file if needed)
*.sqlite

# Crash dumps (Windows)
*.mdmp

# macOS noise (if repo used cross‑platform later)
.DS_Store

# Rule packs local overrides
rules/*.generated.yaml

# Custom tool indexes
AppData/Local/AppLocator/index.db
**/AppLocator/index.db

# Temporary index folders created during local experimentation / tests
/tempidx/
/tempidx2/
/tempidx*/

# Root-level index cache file (if accidentally created during manual runs)
/index.json

# Packaging
*.nupkg
*.snupkg

# JSON source-gen caches
**/obj/*/AppLocate.Cli.JsonContext.g.cs

# Ignore test host artifacts
**/TestResults/

# Rider / ReSharper
_ReSharper*/
*.DotSettings.user

# T4 / codegen (if added later)
*.gen.cs

# Ensure we keep YAML rules
!rules/*.yaml
